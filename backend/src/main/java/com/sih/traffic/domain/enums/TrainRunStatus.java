package com.sih.traffic.domain.enums;

/** Derived (not persisted) runtime status of a train within a simulation run. */
public enum TrainRunStatus {
    NOT_STARTED,
    RUNNING,
    HALTED,
    COMPLETED
}
